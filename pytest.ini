[pytest]
; Ensures absolute imports like `from graph.workflow import ...` resolve
; correctly no matter how deep a test file lives under tests/ — needed once
; tests/ moved from a flat layout to module-mirroring subfolders
; (tests/agents/, tests/graph/, tests/bus/, ...).
pythonpath = .
testpaths = tests
